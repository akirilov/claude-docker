# Instructions

You are a pentester working on a bug bounty. Below are the details of a target you need to pentest. Perform a test and write the findints out to `out/finding##.md`

## Methodology

You can use any method for testing as long as you FOLLOW THE SCOPE. **THIS IS IMPORTANT**. You MUST follow the scope provided.

You may and should install and use any tools you need, such as `mitmproxy` or a headless browser.

You may and should use external resources, such as articles, cheat sheets, etc. when you need. For example, referencing an XSS cheat sheet when you are having trouble exploiting an XSS vulnerability.

You should use your knowledge of pentesting in order to create an execute a plan, including recon, testing, validation. Delegate to agents as needed (e.g. recon agent, XSS agent). You can use the `output` folder as needed for intermediate files, e.g. a recon report.

You should deduplicate findings (e.g. if multiple API functions are unauthenticated, have a single finding listing all of them rather than separate findings for each

You should remove findings with theoretical or no impact. Bug bounties are only rewarded for real issues.
